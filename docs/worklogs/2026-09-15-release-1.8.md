# Cupcake Chat 1.8.0 local review candidate

Owner scope: complete locally on `master`; no push, production signing, publication, or release
workflow execution before review. Later owner instructions supersede the original handoff's old
name/version, VM, zoom-testing, demo topics, permission presets, and populated owner-profile rules.

## Final product

Final product source: **9bbad6c**. MIT licensing and its bundled notice are from **ff5951a**;
**0757ce7** adds smooth streaming, stable chat headers and inline Python tests. **7bd6648** renders
provider display equations. **7c40e0c** removes the chat paragraph width cap; **9bbad6c** fixes
saved code reuse and Home project counts. The revised media supersedes the long take described
below.

- Cupcake Chat 1.8.0 branding, prompt-free startup, full-freedom default, coherent theme surfaces,
  readable contrasting message roles, compact navigation, fast tooltips, and reduced-motion support.
- Skippable and resumable provider/local-runtime onboarding, connected route discovery, empty draft
  cleanup, Recent's in-place all-chats toggle, working project cards, and versioned artifacts.
- Original generated wallpapers and avatars, including six historical scenes. Roman cupcake camp is
  the final default; Strawberry, red/pink scenes and other colors remain available.
- 24 detailed historical advisors across six eras, plus 15 everyday advisors. Seeding preserves
  edits. Portraits depict expressive living cupcake characters rather than human soldiers or plush.
- Offline map travel, siege stores, and horse-forage calculators with visible assumptions and
  artifact saving. History workshop has its own Tools tab; native tool search/categories stay above
  the results. These calculators are UI features, not newly exposed agent-callable native tools.
- MIT License selected by the owner; manual draft-release workflow and signed-update verification
  implemented.

## Current verification

Evidence root: `E:\temp\cupcake-release-1.8-20260915`.

| Check                   | Evidence / result                                                                                                                                                                                     |
| ----------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Final Python sidecars   | `portrait-sidecars-fresh.log`, successful freeze with 39 default advisors                                                                                                                             |
| Final NSIS installer    | `final-bundle-resumed.log`, successful pre-MIT bundle at product source 9a5b9f2                                                                                                                       |
| Bundle smoke            | `final-smoke.log`, passed                                                                                                                                                                             |
| Candidate audit         | `final-audit.log`; `out/final-candidate-audit.json`, passed                                                                                                                                           |
| Native startup          | Prior host startup `history-final-start/startup.json`: Home in 4569 ms. Fresh-copy cold start `portrait-media-start/startup.json`: Home in 16540 ms; final owner/reopen timing is recorded separately |
| Native release checks   | `portrait-native-verified/release-1.8-verification.json`, passed identity, default advisors, prompt depth, onboarding resume and unsigned updater state                                               |
| Native History workshop | `history-ui/receipt-native.json`, passed search placement, own tab, six eras, 125 map polygons, portraits, responsive calculations and artifact save                                                  |
| Desktop tests           | `history-vitest.log`, 122 passed                                                                                                                                                                      |
| Targeted runtime tests  | `portrait-runtime-tests.log`, 67 passed                                                                                                                                                               |
| Type/lint               | `history-typecheck-final.log`, `toolbar-typecheck.log`, `history-eslint.log`; new catalog Ruff passed                                                                                                 |
| Real local model        | `history-local-reviewed.json`, `history-curated-local.json`, app-managed Qwen3 8B CUDA 13, NVIDIA process observed, actual persisted outputs, unload                                                  |
| Native artifact         | `siege-native.json`, native.sandbox.python, contained packaged worker, six tests passed                                                                                                               |
| README screenshots      | `readme-media.json`, six packaged screens, all opened for inspection                                                                                                                                  |

The siege script is `scripts/fixtures/siege-supplies.py`, SHA-256
`f51a029dfbc41626c4272eabc9be539f95368a6fd401d260fc0a95db41981f13`. Its declared 18,000 kg stock /
10% spoilage / 600 people / 0.75 kg ration / 120 arrivals at day 10 scenario yields 31.67 days. It
is not asserted to describe an actual historical siege.

Earlier broader suites passed 189 root tests, 549 runtime tests with one skip, 31 native-host tests,
and 143 broker tests with one documented ignored probe. Those are prior-revision evidence, not
claims that the entire suite was rerun after the historical catalog change. Earlier native tests
also covered broker recovery without document reload, referenced drafts, and local unload/reopen.

## Profiles and media

Owner profile: `E:\temp\cupcakeai-owner-test-20260902`; cleanup was authorized. A verified backup
exists at `E:\temp\cupcake-owner-backups\before-1.8-clear-20260915`. Provider connections and model
files are preserved.

Demo profile: `E:\temp\cupcake-chat-demo-profile-1.8`; three active historical projects and six
active chats, with saved Google, Cohere, Cloudflare and local Qwen3 CUDA output. Weak numerical
answers and older topic examples were archived. Fresh user profiles receive advisors, not chat
fixtures. See the final inventory receipt for exact IDs and artifact counts.

The Gifsmith helper reads the actual Google exchange, records its provenance, intercepts exactly one
filmed Send and replays the saved text. Its normal completion verifies unchanged history; the
recovered final take instead has the narrower checks documented below. Other recorded navigation
uses real controls. The film is a controlled demonstration, not an API latency test. See
[the storyboard](../demo-storyboard.md) and [owner guide](../OWNER_TEST_GUIDE.md).

## Findings and limits

- The updater originally panicked on null configuration. Explicit empty-key configuration now opens
  normally and accurately reports signing is not configured in this local review build.
- Recovery originally left provider state stale; durable state now rehydrates without resending
  work.
- Connected Groq/OpenRouter/Cloudflare routes were excluded unless editorially profiled; ready
  routes now appear while the recommendation shelf remains curated.
- A new draft with an artifact reference failed REFERENCE_UNAVAILABLE; Send now creates its scope
  before resolving references. Attaching context alone still does not save an empty chat.
- Tool search moved too far down after adding the workshop; a separate top-level workshop tab fixes
  the layout, verified in the final packaged app.
- Several later provider requests failed with rate limits or availability errors. They are not
  counted as successful demo evidence. Historical prose remains model output rather than verified
  scholarship; calculations use independently tested assumptions.
- Content encryption remains automatic. A content-encryption-off migration is not implemented;
  password-free opening is implemented. This is recorded rather than claiming a password toggle
  disables SQLCipher or object encryption.

An isolated signed 1.8.1 test-feed download succeeded and a tampered payload was rejected:
`E:\temp\cupcake-chat-updater-tests\run-1789415380691-28676\evidence.json`. Production NSIS upgrade
and cross-version profile retention remain unqualified. The owner waived VM testing. The final local
installer is unsigned; production updater keys and release workflow execution require review.

## Portrait correction after film review

Frame inspection exposed a native-only omission: the new portraits rendered in the picker but the
settings validator rejected their `product:art/history/` identifiers. Commit `9a5b9f2` accepts only
registered built-in persona assets alongside the existing avatar sources. Unknown assets and path
traversal remain rejected. The regression test cycles all 24 portraits through both settings and
reopens the profile. `portrait-runtime-tests.log` records 67 passing targeted tests.

The attach-only native verifier now clicks both historical portrait choices, checks the active
selection, reloads, verifies the stored identifiers and restores the prior choices. The film helper
also requires both selections to succeed, so a rendered but unsaved portrait cannot pass unnoticed.
The older take was moved to `E:\uesless` and is not a review deliverable.

The first portrait package mistakenly reused the previous frozen Python sidecar. The packaged
selection check failed and caught it; that package was excluded. The corrected freeze used the full
`package-sidecars.mjs` command. The source snapshot's `application.py` SHA-256 is
`6d2c7fa334573d492ab73a6246a5ae8af99e61b249534f8003a9e7a67156dd01`.
`portrait-native-verified/release-1.8-verification.json` passes the real picker save/reload check.
Recording uses a packaged host copy with this fresh, verified sidecar set so installer rebuilding
can proceed without locking the recording process. `media-build-hashes.json` identifies that copy.

## Restart recovery

Windows restarted after the final Gifsmith capture completed, while PNG pacing and the installer
build were active. All 19,648 captured JPEG frames and the concat timeline survived. The installer
build was resumed, and the MP4 was encoded directly from the complete Gifsmith timeline.
`recovery-evidence.json` verifies both featured messages against the exact pre-recording export
after reopening the native profile. The lost in-memory Send counter and whole-history pre-capture
digest are explicitly unavailable. `post-reboot-demo-inventory.json` confirms three projects, six
active conversations, eight artifacts, and no loaded local model.

## Final local installer lifecycle and licensing

The owner selected MIT after the first completed package. Commit ff5951a replaces the root license,
updates JavaScript, Rust and Python metadata, changes the About label, and includes LICENSE as an
installer resource. The standard notice follows https://opensource.org/license/mit. Third-party
notices are unchanged. The Python sidecars and NSIS installer are rebuilt with these inputs.

The pre-MIT silent NSIS install used /S /NS in E:\temp\cupcake-chat-installed-1.8. The executable
matches the loose candidate except for Tauri's expected three-byte NSS/UNK bundle marker; this was
confirmed byte for byte rather than assuming an unexplained hash mismatch was safe. Native identity,
39 advisors, portrait save/reload, onboarding resume and updater state passed in the installed
executable. Evidence: installer-install.json and installed-owner-verified/.

installer-uninstall.json records exit 0, removal of the new uninstall/protocol registration,
unchanged legacy CUPCAKEAGI registration, and unchanged hashes for all 89 non-model profile files.
owner-post-uninstall-inventory.json confirms an empty active workspace and 39 default advisors after
reopening. Historical projects and artifacts remain in the separate demo profile. Old owner examples
were archived, not irreversibly deleted; completed task audit records remain.

The superseded long MP4 is 338.37 seconds, 1440 × 920, 30 fps H.264, 19,420,229 bytes. The 15-second
GIF is 1,081,882 bytes. All six screenshots, 15-second film overview frames, and closer portrait,
model picker, forage and onboarding sequences were inspected. readme-render.json reports both
requested main sections, no failed resources and no horizontal overflow. The top/user/contributor
renders were opened and reviewed. No audio track is included.

## Quiet first-release update status

Owner feedback replaced the large configuration warning with a compact App updates row and "Updates
start with the first release." Commit 93064a6 changes presentation only: an unconfigured preview
still cannot claim an up-to-date check or download an unverifiable update. Signed release checks,
downloading and installation behavior are unchanged. quiet-updater-tests.log records all 122 desktop
tests passing; quiet-updater-typecheck.log passes. The final native verifier expects the compact
status and no unavailable/configuration-warning wording.

The superseded film omits original timestamps 296.000–301.000, where the obsolete warning was
visible. The original complete take remains in the private capture directory with its recovery
provenance. That old MP4 retained all demo chapters and contains no replacement model output.

## Streaming and conversation-tool correction

Owner feedback required faster filming, real code generation inside chat, no bottom-right captions,
one wallpaper change, visible advisor interactions, and a stable chat title during scrolling.
Evidence for this continuation is under `E:\temp\cupcake-chat-smooth-demo-1.8`.

The original replay exposed saved turns before hiding them and revealed text nodes inside an
already-complete DOM, leaving empty bullets visible. The replacement hides rows before navigation,
reveals the user only after the filmed Send click, and streams actual saved Markdown through the
production renderer. Its initial short take recorded 1,439 samples with no premature-user or
empty-list frames. The final capture also checks both saved history hashes.

The app now smooths transport chunks with a grapheme-aware presentation buffer, bounded to 160 ms of
catch-up. Authoritative text and stored responses are unchanged. A stable render key survives
run-to-message ID replacement and history refresh. The completion handler also avoids briefly
appending the same final assistant twice. Stable code-block components preserve their buttons and
results when workspace events or text updates rerender the conversation.

`overflow: hidden` still allowed programmatic scrolling of the outer chat surface; `overflow: clip`
prevents that displacement. In the installed app the title stayed at 40.29 px before and after
message scrolling, focus scrolling, and an attempted outer scroll. Its outer scroll position stayed
zero. This checks the normal desktop viewport without zoom or repeated size sweeps.

Run tests on a completed Python code block saves or reuses an artifact, executes its immutable
revision through the existing native Python task pipeline, and shows the real output inside chat.
The model's original grain code failed one test (18 days versus an incorrect 24-day expectation). A
model-produced correction passes all eight tests in the native AppContainer sandbox. Rejected
provider responses and the failing task remain auditable; they are not shown as successful work.

A real UI send asking about 400 people produced 45 days from the stated assumptions. The installed
stream audit sampled 931 frames with zero empty bullets and zero clipped-title frames. It exposed
raw LaTeX display delimiters, now covered by a renderer regression that converts only standalone
math delimiters outside code fences. All 129 desktop tests and workspace typechecks pass.

The broad lint run also exposed an existing eight-file fallback-project limit in the browser tests.
A dedicated `tests/e2e/tsconfig.json` makes those files a real TypeScript project, preserving typed
linting without raising the fallback limit.

## Final installed build and revised media

Product source **9bbad6c** is the final local candidate. The installed verifier measured all three
assistant paragraphs at 1040.67 px against 1041 px of available content width, with no fixed
maximum. The title remained at 40.29 px during scrolling. Executing the saved grain code passed
eight tests and kept Rome's artifact count at nine before and after. Home correctly showed one
Mongol chat and two Viking chats. Earlier duplicate test artifacts remain in the demo's audit
history; new reruns reuse the existing saved code after verifying its content.

`9bbad6c-tests.log` records 129 passing desktop tests. Workspace lint and typechecks, bundle smoke,
and the candidate audit passed. `mit-installer-install.json` verifies the installed executable
against the loose binary (only Tauri's three-byte bundle marker differs), sidecar manifest, and MIT
notice. The installed demo reached Home in 5.001 seconds; the empty owner profile took 3.011
seconds. `final-owner-verified/` passed default advisors, portrait persistence, onboarding, and
compact update status. `final-owner-inventory.json` contains zero active projects/chats/artifacts,
39 advisors, and no loaded model. The demo has three projects, seven chats, 14 artifacts, and 41
personas including its two custom discussion members. Provider credentials and local downloads
remain in their profiles.

The final silent uninstall removed the disposable installation and its protocol/registration,
preserved all 89 non-model owner files byte for byte, and left the older CUPCAKEAGI installation
unchanged. Both owned app process trees closed completely. The GPU marker remains `no`.

`approved-take/evidence.json` records a completed 97.8-second Gifsmith capture, two intercepted
replay Sends, zero premature-user frames, zero empty-list frames, and unchanged saved-history
hashes. The 84.8-second edit removes original seconds 47–60 of actual Python waiting. The final cut,
code/result sequence, advisor/avatar interactions, overall chapter frames, all six screenshots, and
README top/user/contributor renders were visually inspected. This is sampled temporal review, not a
claim that every encoded frame was individually inspected. README rendering reported no failed
resources or horizontal overflow. The MP4 preview was queued in Codex for the owner.

Final media: H.264 MP4, 1440 × 920, 30 fps, 17,118,268 bytes; animated GIF, 840 × 537, 10 fps,
21,685,111 bytes. Both last 84.8 seconds and have no audio. Only the GIF and six PNG screenshots are
in `docs/media`; the MP4 stays at `E:\temp\cupcake-chat-review-1.8.mp4`.

Final loose executable SHA-256: `42ced763a1007b84067ac6c22550d67dfdbe7e614ae59e7b79c93faf7b846e51`.
Final unsigned installer SHA-256:
`19cd634e279107276497302cc38ca1716c630821f6b93161a59a8c7680168f08`. The installer is 88,588,301
bytes. `final-deliverable-hashes.json` records file paths and sizes. The owned-file secret scan
passed without exposing credential values.

Production signing, publication, a production updater endpoint, and cross-version upgrade
qualification remain outside this local review. No push, tag, release workflow, or publication was
performed. The original owner-requested v1.0.0 baseline and unrelated dirty files remain preserved.

Disposable frame directories from the rehearsal and final take were moved to
`E:\uesless\cupcake-chat-capture-frames-20260915`. `frame-relocation.json` records both original and
new paths. Encoded source movies, review strips, receipts, and final deliverables remain available.
The superseded anime-recorder edits were saved there before restoring that legacy tracked helper;
the current historical replay is `scripts/demo-cupcake-chat-replay.mjs`.

## Owner review correction: saved follow-up and Recent navigation

The owner identified an untyped user bubble near 18 seconds in the 84.8-second edit. Inspection
confirmed that `restoreReplay` revealed the saved follow-up "What happened when the supplies stopped
arriving?" before the Memory navigation. The earlier audit stopped when streaming ended and missed
this transition; its zero counts did not establish that later frames were free of unexpected turns.

The recorder now restores the first answer while retaining the later-turn visibility filter until
the conversation is offscreen. The audit continues through that transition and checks unexpected
user rows after Send as well as premature rows before Send. The cleanup helper also waits for the
conversation surface to unmount. Projects appears once; all three chats are selected directly from
Recent, with All chats left enabled for the group conversation.

`recent-take/evidence.json` passed both replay audits with zero premature, unexpected-user, and
empty list frames. Its captured click log contains exactly one Projects navigation and three Recent
selections. Both saved history digests remained unchanged. The completed capture lasts 92.6 seconds;
the revised edit removes original seconds 44–57 of Python waiting and lasts 79.6 seconds. The
16–20-second transition and real test-result boundary were visually inspected. This replaces the
previous review movie and README GIF; product source and installer remain **9bbad6c**.

The revised private review file is `E:\temp\cupcake-chat-review-1.8-recent.mp4`. No new model
inference was used. The headless packaged app was restored to its Roman Home scene and closed with
its owned child processes. No push or publication occurred.
